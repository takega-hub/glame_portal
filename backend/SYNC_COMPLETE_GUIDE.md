# Полное руководство по синхронизации с 1С

## Обзор

Система синхронизирует данные покупателей из 1С УНФ ФРЕШ, включая:
- ✅ Дисконтные карты и контактные данные
- ✅ **Начальный ввод** (миграция из старой системы)
- ✅ Историю покупок за выбранный период
- ✅ Бонусные баллы

## Этапы полной синхронизации

### 1. Синхронизация дисконтных карт (10-40%)
**Источник:** `Catalog_ДисконтныеКарты`

**Что синхронизируется:**
- Номер телефона (из поля `Code`)
- ID дисконтной карты (`Ref_Key`)
- ID контрагента (`Владелец_Key`)
- Данные контрагента (ФИО, email, **город**)

**Город извлекается из:**
1. **Приоритет 1:** Поле `ОсновныеСведения` (3-я строка или элемент)
2. **Приоритет 2:** Структура `Состав → АдресРФ → Улица`
3. **Приоритет 3:** Прямые поля: `Город`, `City`, `НаселенныйПункт`

**Файл:** `backend/app/services/customer_sync_service.py`  
**Метод:** `sync_discount_cards()`

---

### 2. Синхронизация начального ввода (42-50%)
**Источник:** `AccumulationRegister_ПродажиПоДисконтнымКартам_RecordType`  
**Фильтр:** `Recorder_Type` содержит `ВводНачальныхОстатков`

**Что синхронизируется:**
- Записи переноса из старой системы (`Document_ВводНачальныхОстатков`)
- **Без ограничения по дате** (в отличие от обычной синхронизации покупок)
- Сумма переноса берется из поля `Сумма` в записи регистра
- Артикулы товаров для связи с каталогом GLAME

**Пример:**
```
Покупатель: Царькова 79788451364
Начальный ввод: 232,360.00 руб (перенос из старой системы)
```

**Файл:** `backend/app/services/customer_sync_service.py`  
**Метод:** `sync_initial_balances()`

---

### 3. Синхронизация истории покупок (55-85%)
**Источник:** `AccumulationRegister_ПродажиПоДисконтнымКартам_RecordType`  
**Фильтр:** По дисконтной карте за выбранный период (по умолчанию 365 дней)

**Что синхронизируется:**
- Дата покупки (`Period`)
- Сумма (`Сумма`)
- Количество (`Количество`)
- Артикул товара (из `Catalog_Номенклатура`)
- Связь с товаром в каталоге GLAME (по `external_id` или `article`)

**Fallback:** Если нет данных по карте, используется `AccumulationRegister_Продажи_RecordType` по контрагенту.

**Исключения:** Товары упаковки (Мешочек, Салфетка, Пакет, Коробка и т.д.) **не связываются** с каталогом, но синхронизируются.

**Файл:** `backend/app/services/customer_sync_service.py`  
**Метод:** `sync_purchase_history(days=365)`

---

### 4. Синхронизация бонусных баллов (90-98%)
**Источник:** `AccumulationRegister_БонусныеБаллы_RecordType`

**Как рассчитываются баллы:**
- Берется **последняя запись** по дисконтной карте (сортировка: `Period desc`)
- Используется поле **`КСписанию`** (доступно к списанию)
- **НЕ используется** `Начислено - КСписанию`

**Пример:**
```json
{
  "Period": "2024-11-15T21:00:00",
  "Начислено": 7333,
  "КСписанию": 4806  ← Это значение сохраняется
}
```

**Файл:** `backend/app/services/onec_customers_service.py`  
**Метод:** `fetch_loyalty_balance()`

---

## Итоговые данные покупателя

### Пример: Царькова 79788451364

| Показатель | Значение |
|---|---|
| **Город** | Симферополь |
| **Начальный ввод** | 232,360.00 руб |
| **Покупки за год** | 132,694.00 руб |
| **ИТОГО покупок** | 365,054.00 руб ✅ |
| **Бонусные баллы** | 4,806 баллов |
| **История покупок** | 10 записей (с артикулами) |

---

## Запуск синхронизации

### Через API
```bash
curl -X POST "http://localhost:8000/api/admin/onec/customers/sync/full?limit=1000&days=365" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Параметры:**
- `limit` (по умолчанию 1000): Максимум дисконтных карт для синхронизации
- `days` (по умолчанию 365): Период истории покупок (начальный ввод синхронизируется всегда)

### Через UI
1. Перейти в админ-панель
2. Раздел "Синхронизация с 1С"
3. Нажать "Полная синхронизация покупателей"
4. Следить за прогресс-баром в реальном времени

### Проверка конкретного покупателя
```bash
cd backend
python test_customer_data.py --phone 79788451364
```

**Вывод:**
- Данные из 1С (город, баллы, сумма покупок)
- Данные из БД (для сравнения)
- Анализ связи товаров с каталогом GLAME
- Документы начального ввода

---

## Ключевые файлы

| Файл | Описание |
|---|---|
| `backend/app/services/customer_sync_service.py` | Основная логика синхронизации |
| `backend/app/services/onec_customers_service.py` | API-клиент для 1С |
| `backend/app/api/admin/onec_customers.py` | REST API endpoints |
| `backend/app/services/sync_task_manager.py` | Управление фоновыми задачами |
| `backend/test_customer_data.py` | Тестовый скрипт проверки данных |
| `frontend/src/app/admin/customers/page.tsx` | UI админ-панели |

---

## Важные изменения (2026-02-04)

### ✅ Добавлено
1. **Синхронизация начального ввода** (`sync_initial_balances`)
   - Отдельный этап без ограничения по дате
   - Записи типа `Document_ВводНачальныхОстатков`
   - Исправлена проблема с неполной суммой покупок

2. **Город покупателя**
   - Приоритетное извлечение из `ОсновныеСведения`
   - Fallback на структуру адреса
   - Добавлена колонка `city` в БД

3. **Правильный расчет бонусных баллов**
   - Используется поле `КСписанию`
   - Берется последняя запись (не сумма)

### 🔧 Исправлено
- Фильтрация по дате больше не исключает начальный ввод
- Поиск работает по подстрокам (включая город)
- Устранены ошибки `MissingGreenlet` и timezone-aware datetime

---

## Дополнительно

### Миграции БД
```bash
cd backend
alembic upgrade head
```

Или вручную:
```bash
cd backend
python sync_db_schema.py
```

### Переменные окружения
```env
ONEC_API_URL=https://your-1c-server/odata/standard.odata
ONEC_API_TOKEN=Base64EncodedBasicAuth
ONEC_LOYALTY_ENABLED=true
```

### Проверка логов
```bash
# Логи синхронизации
tail -f backend/logs/sync.log

# Запуск тестового скрипта
cd backend
python test_customer_data.py --phone 79788451364
```

---

## Известные ограничения

1. **Товары упаковки** не связываются с каталогом (исключены из анализа)
2. **Период покупок** ограничен 365 днями (кроме начального ввода)
3. **1C OData API** имеет ограничения на размер выборки (`$top=10000`)

---

## Поддержка

При возникновении проблем:
1. Проверьте логи синхронизации
2. Запустите тестовый скрипт для конкретного покупателя
3. Убедитесь, что переменные окружения настроены
4. Проверьте доступность 1С API

---

**Дата обновления:** 2026-02-04  
**Версия:** 2.0  
**Статус:** Проверено и подтверждено ✅
