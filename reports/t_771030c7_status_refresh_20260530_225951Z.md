# t_771030c7 — актуализация передачи Анатолию

Дата проверки UTC: 2026-05-30 22:59:51Z

## Контекст

Задача: передать Анатолию техническое ТЗ на дашборд магазинов и продавцов с корректными планами.

Критичная бизнес-правка Елены: ссылка Яндекс.Диска https://disk.yandex.ru/d/w9ahxGKuAN_Hag относится к плану Ялты, не к ТРК Центрум. Ее нельзя использовать как план Центрума.

Повторную отправку в Telegram не делал, чтобы не спамить: в истории карточки уже есть подтвержденные DM Анатолию, последний зафиксированный message_id=6987.

## Read-only API verification

Команды выполнены из `/workspace/glame-platform`, секреты не печатались.

1. `python /workspace/tools/glame_api.py env`
   - GLAME_ENV=development
   - GLAME_API_BASE_URL=http://172.17.0.1:18000
   - GLAME_API_TOKEN / GLAME_AUTH_USERNAME / GLAME_AUTH_PASSWORD присутствуют, значения скрыты helper-ом.

2. `GET /health`
   - HTTP 200 OK
   - `{ "status": "healthy" }`

3. `GET /api/admin/1c/sellers/kpi/dashboard?month=2026-05`
   - HTTP 200 OK
   - `data_quality.duplicate_store_rows = 0`
   - `data_quality.seller_field_status = ok`
   - `data_quality.unmatched_sellers = 0`
   - total revenue = 2 122 512.05
   - total revenue_plan = 4 569 987.52
   - total completion_percent = 46.44
   - Ялта, Набережная 18: fact 1 490 561.95 / plan 2 854 371.52
   - ТРК Центрум: fact 631 950.10 / plan 1 715 616.00

4. `GET /api/analytics/1c-sales/details?period=month&limit=5`
   - HTTP 200 OK
   - `data_warnings = []`
   - sample rows: 5
   - sample matched seller rows: 5/5
   - sample unmatched seller rows: 0/5
   - `seller_name` is null in 4/5 sample rows, so frontend/API must keep fallback to `seller_external_id`.
   - row fields include: `check_id`, `document_id`, `seller_id`, `seller_external_id`, `seller_name`, `seller_match_status`, `seller_unmatched`, `store_id`, `store_name`, `sale_date`, `product_name`, `product_brand`, `product_category`, `revenue`, `quantity`.

## Local checks

`python -m py_compile backend/app/api/analytics.py backend/app/api/admin/onec_customers.py backend/app/services/seller_kpi_service.py` — OK.

Relevant git state remains dirty / review-gated:

- `backend/app/api/admin/onec_customers.py` modified
- `backend/app/api/analytics.py` modified
- `frontend/src/components/analytics/SalesPanel.tsx` modified
- `backend/app/services/seller_kpi_service.py` untracked

## Вывод

Передача Анатолию уже выполнена и подтверждена историей карточки. Свежая API-проверка подтверждает, что backend сейчас разделяет планы Ялты и ТРК Центрум, а sales details содержит seller/check поля и seller matching status.

Не закрывать как Done без финального технического acceptance Анатолия и UI-проверки plan-source/status layer: в дашборде должны быть видны `source`, `period`, `store`, `matched_store_id`, `match_status`, а для неподтвержденного плана Центрума должен быть warning вместо ложного перевыполнения.
