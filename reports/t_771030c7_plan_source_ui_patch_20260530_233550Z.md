# t_771030c7 — plan-source/dashboard follow-up

Дата: 2026-05-30 23:35:50Z

## Что сделано

Выполнена точечная доработка слоя дашборда магазинов/продавцов, чтобы закрыть оставшийся acceptance-пункт по источникам планов:

- `backend/app/services/seller_kpi_service.py`
  - store rows в `/api/admin/1c/sellers/kpi/dashboard` теперь формируют provenance для плана выручки:
    - `revenue_plan_source`
    - `revenue_plan_period`
    - `revenue_plan_store`
    - `revenue_plan_matching_status`
    - `revenue_plan_source_detail`
  - добавлен helper `_target_metric_plan_sources(...)`.
  - добавлены `data_quality.plan_warnings[]`, если у магазина нет подтвержденного/загруженного плана.

- `frontend/src/lib/api.ts`
  - добавлены TS-типы для plan source detail и plan warnings.

- `frontend/src/components/profile/SellerKpiMainDashboard.tsx`
  - в карточке каждого магазина показаны источник плана, период, магазин и статус сопоставления.
  - в блоке «Качество данных» выводится `Data-warning по плану`, если backend сообщает missing/unconfirmed plan.

## Read-only проверка live API до deploy/restart

- `GET /health` → HTTP 200 OK.
- `GET /api/admin/1c/sellers/kpi/dashboard?month=2026-05` → HTTP 200 OK.
- Live данные по магазинам по-прежнему разделены:
  - `Ялта, Набережная 18`: fact `1 490 561.95`, plan `2 854 371.52`, completion `52.22%`.
  - `ТРК Центрум`: fact `631 950.10`, plan `1 715 616.00`, completion `36.84%`.
- `data_quality`: `duplicate_store_rows=0`, `seller_field_status=ok`, `unmatched_sellers=0`.
- Live backend еще не отдает новые plan-source поля после source patch (`revenue_plan_source`/`period`/`matching_status` null), значит нужен reload/deploy backend перед финальной API/UI верификацией.

## Проверка кода

- `python -m py_compile backend/app/services/seller_kpi_service.py backend/app/api/analytics.py backend/app/api/admin/onec_customers.py` → OK.
- `cd frontend && npx eslint src/components/profile/SellerKpiMainDashboard.tsx src/lib/api.ts` → OK.
- `cd frontend && npx tsc --noEmit --pretty false` → OK.
- `git diff --check -- backend/app/services/seller_kpi_service.py frontend/src/lib/api.ts frontend/src/components/profile/SellerKpiMainDashboard.tsx` → OK.

## Ограничения / review gate

- Рабочее дерево `/workspace/glame-platform` остается сильно dirty до этой задачи; правки нужно смотреть точечно по указанным файлам.
- Не выполнял restart/deploy backend самостоятельно, чтобы не делать production-risk действие без review Анатолия.
- Карточку нельзя закрывать как Done до:
  1. review/redeploy backend;
  2. повторной проверки, что live dashboard API отдает plan-source fields;
  3. UI-проверки, что магазинные карточки показывают source/period/store/status и warning при missing/unconfirmed plan.

## Критичная бизнес-правка

Ссылка Яндекс.Диска `https://disk.yandex.ru/d/w9ahxGKuAN_Hag` относится к Ялте, не к ТРК Центрум. План Ялты нельзя использовать как план Центрума.
