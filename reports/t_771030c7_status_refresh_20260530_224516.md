# t_771030c7 — актуализация статуса

Дата проверки: 2026-05-30T22:45:16Z

## Задача
Передать Анатолию ТЗ и технический контекст по dashboard магазинов/продавцов с корректными планами и seller-level данными.

## Статус по истории карточки
- Handoff Анатолию уже выполнен ранее через Telegram; последний зафиксированный DM в комментариях карточки: `message_id=6987`.
- Повторно не отправлял, чтобы не спамить: содержание задачи и критичная поправка уже доставлены.
- Карточка остаётся `blocked`, потому что нужен внешний review/acceptance от Анатолия и точечная проверка UI/plan-source layer перед Done.

## Свежая read-only верификация API

### Health
- `python /workspace/tools/glame_api.py get /health` → exit 0 / HTTP 200 OK.

### Seller KPI dashboard
- `GET /api/admin/1c/sellers/kpi/dashboard?month=2026-05` → exit 0 / HTTP 200 OK.
- `data_quality`:
  - `duplicate_store_rows=0`
  - `seller_field_status=ok`
  - `unmatched_sellers=0`
- Магазины разделены:
  - `ТРК Центрум`: fact `631950.10`, plan `1715616.00`, completion `36.84%`.
  - `Ялта, Набережная 18`: fact `1490561.95`, plan `2854371.52`, completion `52.22%`.
- Totals: revenue `2122512.05`, plan `4569987.52`, completion `46.44%`.

### Детализация продаж
- `GET /api/analytics/1c-sales/details?period=month&limit=5` → exit 0 / HTTP 200 OK.
- Sample: 5 строк, 5/5 matched, 0 unmatched, `data_warnings=[]`.
- В строках есть необходимые поля:
  - `check_id`, `document_id`
  - `seller_id`, `seller_external_id`, `seller_name`, `seller_match_status`, `seller_unmatched`
  - `store_id`, `store_name`, `sale_date`, `product_name`, `product_brand`, `product_category`, `revenue`, `quantity`
- Ограничение: `seller_name` отсутствует в 4/5 sample строк, поэтому UI/API должны сохранять fallback на `seller_external_id` и показывать статус качества данных.

### Syntax/static sanity
- `python -m py_compile backend/app/api/analytics.py backend/app/api/admin/onec_customers.py backend/app/services/seller_kpi_service.py` → OK.
- `git diff --check` по релевантным файлам → без вывода ошибок.

## Критичная бизнес-правка
Ссылка Яндекс.Диска `https://disk.yandex.ru/d/w9ahxGKuAN_Hag` относится к плану Ялты, не к ТРК Центрум. Её нельзя использовать как план Центрума.

## Вывод
Передача Анатолию выполнена и базовые backend/API acceptance-пункты сейчас проходят по read-only проверке: планы Ялты/Центрума разделены, seller diagnostics присутствуют, детализация продаж отдаёт seller/check поля. Не закрывать карточку в Done без подтверждения Анатолия и UI-проверки plan source/status warnings (`source`, `period`, `store`, `matched_store_id`, `match_status`, warning для неподтверждённого плана Центрума).
