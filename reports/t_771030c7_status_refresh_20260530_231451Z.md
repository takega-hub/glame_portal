# t_771030c7 — свежая проверка передачи Анатолию

Дата проверки: 2026-05-30T23:14:51Z

Повторную отправку Анатолию не выполнял, чтобы не спамить: в истории карточки уже есть подтвержденная Telegram DM-передача, последний зафиксированный message_id=6987.

## Read-only API verification

- `python /workspace/tools/glame_api.py env` — GLAME API env присутствует, секреты замаскированы helper'ом.
- `GET /health` — HTTP 200 OK.
- `GET /api/admin/1c/sellers/kpi/dashboard?month=2026-05` — HTTP 200 OK.
  - `data_quality.duplicate_store_rows = 0`
  - `data_quality.seller_field_status = ok`
  - `data_quality.unmatched_sellers = 0`
  - Ялта, Набережная 18: fact `1 490 561.95`, plan `2 854 371.52`, completion `52.22%`.
  - ТРК Центрум: fact `631 950.10`, plan `1 715 616.00`, completion `36.84%`.
- `GET /api/analytics/1c-sales/details?period=month&limit=5` — HTTP 200 OK.
  - sample `5/5` matched, `0/5` unmatched.
  - `data_warnings=[]`.
  - строки содержат обязательные поля: `check_id`, `document_id`, `seller_id`, `seller_external_id`, `seller_name`, `seller_match_status`, `seller_unmatched`, `store_id`, `store_name`, `sale_date`, `product_name`, `product_brand`, `product_category`, `revenue`, `quantity`.
  - ограничение: `seller_name` пустой в `4/5` sample строк, поэтому UI должен использовать fallback на `seller_external_id` и показывать data-quality статус.

## Static verification

- `python -m py_compile backend/app/api/analytics.py backend/app/api/admin/onec_customers.py backend/app/services/seller_kpi_service.py` — OK.

## Вывод

Backend/API acceptance-пункты по разделению Ялты/Центрума и seller diagnostics сейчас выглядят подтвержденными. Критичная бизнес-правка сохраняется: ссылка `https://disk.yandex.ru/d/w9ahxGKuAN_Hag` — это план Ялты, не план ТРК Центрум.

Карточку нельзя честно закрывать как Done без финального acceptance Анатолия и UI-проверки слоя `source`, `period`, `store`, `matched_store_id`, `match_status`, warning для неподтвержденного плана Центрума.
