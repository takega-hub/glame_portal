# 🚀 Быстрый старт - Интеграция аналитики

## ⚡ За 5 минут

### 1. Настройте переменные окружения (2 минуты)

Добавьте в ваш `.env` файл (см. `ENV_VARIABLES_TEMPLATE.txt` для полного списка):

```env
# Минимальная конфигурация для тестирования
YANDEX_METRIKA_COUNTER_ID=your_counter_id
YANDEX_METRIKA_OAUTH_TOKEN=your_token

INSTAGRAM_ACCESS_TOKEN=your_token
INSTAGRAM_BUSINESS_ACCOUNT_ID=your_account_id

VK_ACCESS_TOKEN=your_token
VK_GROUP_ID=your_group_id

TELEGRAM_BOT_TOKEN=your_token
TELEGRAM_CHANNEL_USERNAME=your_channel
```

### 2. Примените миграцию БД (1 минута)

```bash
cd backend
alembic upgrade head
```

### 3. Запустите сервисы (1 минута)

**Backend:**
```bash
cd backend
uvicorn app.main:app --reload
```

**Frontend:**
```bash
cd frontend
npm run dev
```

### 4. Откройте Dashboard (1 минута)

Перейдите на http://localhost:3000/analytics

Нажмите "Синхронизировать" на каждой панели для загрузки данных.

## 📚 Полная документация

- **[IMPLEMENTATION_SUMMARY.md](./IMPLEMENTATION_SUMMARY.md)** - сводка реализации
- **[ANALYTICS_INTEGRATION_README.md](./ANALYTICS_INTEGRATION_README.md)** - полная инструкция по настройке
- **[ANALYTICS_API_EXAMPLES.md](./ANALYTICS_API_EXAMPLES.md)** - примеры использования API
- **[ENV_VARIABLES_TEMPLATE.txt](./ENV_VARIABLES_TEMPLATE.txt)** - шаблон переменных окружения

## 🎯 Основные возможности

✅ **6 источников данных:**
- Яндекс.Метрика (веб-аналитика)
- Instagram (соцсети)
- ВКонтакте (соцсети)
- Telegram (мессенджер)
- FTP счетчики (офлайн магазины)
- 1С (продажи)

✅ **Единый Dashboard** с табами для каждого источника

✅ **Объединенная аналитика** - сравнение всех источников

✅ **REST API** для программного доступа

## 🔗 API Endpoints

### Быстрый тест

```bash
# Яндекс.Метрика
curl -X POST "http://localhost:8000/api/analytics/yandex-metrika/sync?days=7"

# Instagram
curl -X POST "http://localhost:8000/api/analytics/instagram/sync?days=7"

# Объединенная аналитика
curl "http://localhost:8000/api/analytics/unified?days=30"
```

Полный список endpoints см. в [ANALYTICS_API_EXAMPLES.md](./ANALYTICS_API_EXAMPLES.md)

## 🎨 Frontend компоненты

Dashboard находится в: `frontend/src/components/analytics/Dashboard.tsx`

Панели:
- `YandexMetrikaPanel.tsx`
- `InstagramPanel.tsx`
- `VKPanel.tsx`
- `TelegramPanel.tsx`
- `StoreVisitsPanel.tsx`
- `SalesPanel.tsx`
- `UnifiedAnalyticsPanel.tsx`

## 🔧 Backend сервисы

Все сервисы в: `backend/app/services/`

- `yandex_metrika_service.py`
- `instagram_service.py`
- `vk_service.py`
- `telegram_service.py`
- `onec_sales_service.py`
- `ftp_service.py` (расширен)

## 📊 Модели данных

- `backend/app/models/social_media_metric.py` - для соцсетей
- `backend/app/models/sales_metric.py` - для продаж

## ⚠️ Важно

1. **API ключи**: Получите ключи для каждого сервиса (см. документацию)
2. **Лимиты**: Учитывайте лимиты API каждой платформы
3. **Срок токенов**: Instagram токен действует 60 дней
4. **Безопасность**: Не коммитьте .env с реальными токенами

## 🆘 Проблемы?

**Ошибка: "Token не настроен"**
→ Проверьте переменные окружения в `.env`

**Ошибка: "Invalid access_token"**
→ Токен истек, получите новый

**Не отображаются данные**
→ Нажмите "Синхронизировать" на панели

**Подробнее см.** [ANALYTICS_INTEGRATION_README.md](./ANALYTICS_INTEGRATION_README.md) раздел "Troubleshooting"

## 📈 Что дальше?

После успешного запуска:
1. Настройте автоматическую синхронизацию (Celery)
2. Добавьте дополнительные платформы (Google Analytics, YouTube)
3. Настройте уведомления об ошибках
4. Оптимизируйте кэширование данных

## ✅ Checklist

- [ ] Переменные окружения настроены
- [ ] Миграция применена
- [ ] Backend запущен
- [ ] Frontend запущен
- [ ] Dashboard открыт
- [ ] Данные синхронизированы
- [ ] Все панели работают

**Готово! 🎉**

Теперь вы можете анализировать данные из всех источников в едином интерфейсе.
