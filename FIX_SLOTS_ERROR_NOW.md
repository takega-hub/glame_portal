# Быстрое исправление ошибки "Ошибка загрузки слотов плана"

## Проблема

На странице `/content-agent` видна ошибка:
```
Ошибка загрузки слотов плана
```

## Причина

Скорее всего, таблица `content_items` не существует в базе данных.

## Решение (3 шага)

### Шаг 1: Перейдите в директорию backend

```powershell
cd backend
```

### Шаг 2: Создайте таблицы

Выполните один из вариантов:

**Вариант A (рекомендуется):**
```powershell
python test_and_create_tables.py
```

**Вариант B:**
```powershell
python create_content_tables.py
```

**Вариант C (через PowerShell скрипт):**
```powershell
.\fix_content_tables.ps1
```

### Шаг 3: Перезапустите backend

1. Остановите backend (Ctrl+C в терминале, где он запущен)
2. Запустите снова:
   ```powershell
   .\start_backend.bat
   ```

### Шаг 4: Обновите страницу

Обновите страницу в браузере (F5) - ошибка должна исчезнуть.

## Что вы должны увидеть

После выполнения скрипта создания таблиц вы должны увидеть:

```
✅ Подключение установлено!
✅ Таблицы созданы!

📋 Итого таблиц: 3
   ✓ content_items
   ✓ content_plans
   ✓ content_publications
```

## Если ошибка сохраняется

### Проверьте логи backend

Посмотрите в терминале, где запущен backend - там будет детальная информация об ошибке.

### Проверьте Docker контейнеры

```powershell
cd infra
docker-compose ps
```

Все контейнеры должны быть в статусе "Up" и "healthy".

### Проверьте таблицы вручную

Если у вас есть доступ к PostgreSQL:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name LIKE 'content_%';
```

Должны быть видны 3 таблицы.

## Улучшения в коде

Теперь endpoint `/api/content/plans/{plan_id}/items`:
- ✅ Показывает понятное сообщение, если таблицы не созданы
- ✅ Предлагает команду для исправления
- ✅ Логирует детальную информацию об ошибке

## После исправления

После создания таблиц:
1. ✅ Ошибка "Ошибка загрузки слотов плана" исчезнет
2. ✅ Вы сможете видеть слоты в таблице
3. ✅ Вы сможете генерировать контент для слотов
