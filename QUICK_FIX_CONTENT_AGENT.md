# Быстрое исправление ошибок Content Agent

## Проблема

На странице `/content-agent` видны ошибки:
- "Ошибка генерации плана"
- "Ошибка загрузки слотов плана"

## Решение

### Шаг 1: Создать таблицы в БД

Выполните команду:

```bash
cd backend
python create_content_tables.py
```

Вы должны увидеть:
```
✅ Подключение установлено
✅ Таблицы успешно созданы!

📋 Созданные таблицы:
   - content_items
   - content_plans
   - content_publications
```

### Шаг 2: Перезапустить backend

После создания таблиц перезапустите backend:

```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
cd backend
.\start_backend.bat
```

### Шаг 3: Проверить работу

1. Обновите страницу в браузере (F5)
2. Попробуйте сгенерировать план
3. Ошибки должны исчезнуть

## Если ошибка сохраняется

### Проверка таблиц

Подключитесь к PostgreSQL и проверьте:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name LIKE 'content_%';
```

Должны быть видны 3 таблицы:
- `content_plans`
- `content_items`
- `content_publications`

### Проверка подключения к БД

```bash
cd backend
python -c "from app.database.connection import engine; print('Connected!' if engine else 'Failed')"
```

### Проверка логов backend

Посмотрите логи backend в терминале, где он запущен. Там будет детальная информация об ошибке.

## Альтернативный способ создания таблиц

Если скрипт не работает, создайте таблицы через SQL напрямую:

```bash
# Через Docker
docker exec -i glame-postgres psql -U glame_user -d glame_db < backend/create_tables.sql

# Или через psql напрямую
psql -h localhost -p 5433 -U glame_user -d glame_db -f backend/create_tables.sql
```

## После исправления

После создания таблиц:
1. ✅ Ошибка "Ошибка генерации плана" должна исчезнуть
2. ✅ Ошибка "Ошибка загрузки слотов плана" должна исчезнуть
3. ✅ Вы сможете генерировать планы и видеть слоты в таблице
