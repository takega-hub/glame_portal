# Исправление ошибок Content Agent

## Проблема

На странице `/content-agent` видны ошибки:
- "Ошибка генерации плана"
- "Ошибка загрузки слотов плана"

## Быстрое решение

### Вариант 1: Использовать PowerShell скрипт (рекомендуется)

```powershell
cd backend
.\fix_content_tables.ps1
```

### Вариант 2: Выполнить Python скрипт напрямую

```powershell
cd backend
python create_content_tables.py
```

### Вариант 3: Проверить статус таблиц

```powershell
cd backend
.\check_tables.ps1
```

## Проверка Docker контейнеров

**Важно:** Docker Compose файл находится в директории `infra`, а не `backend`!

```powershell
cd infra
docker-compose ps
```

Если контейнеры не запущены:

```powershell
cd infra
docker-compose up -d
```

## После создания таблиц

1. **Перезапустите backend:**
   - Остановите текущий процесс (Ctrl+C)
   - Запустите снова: `.\start_backend.bat`

2. **Обновите страницу в браузере** (F5)

3. **Попробуйте сгенерировать план** - ошибки должны исчезнуть

## Проверка результата

После создания таблиц вы должны увидеть в терминале:

```
✅ Подключение установлено
✅ Таблицы успешно созданы!

📋 Созданные таблицы:
   - content_items
   - content_plans
   - content_publications
```

## Если ошибка сохраняется

1. **Проверьте логи backend** - там будет детальная информация об ошибке
2. **Проверьте подключение к БД:**
   ```powershell
   cd backend
   python -c "from app.database.connection import engine; print('Connected!' if engine else 'Failed')"
   ```
3. **Проверьте переменные окружения** в файле `.env`:
   - `DB_HOST`
   - `DB_PORT`
   - `DB_USER`
   - `DB_PASSWORD`
   - `DB_NAME`

## Альтернативный способ (через SQL)

Если скрипты не работают, создайте таблицы через SQL:

```powershell
# Через Docker
docker exec -i glame-postgres psql -U glame_user -d glame_db < backend/create_tables.sql

# Или через psql напрямую (если установлен)
psql -h localhost -p 5433 -U glame_user -d glame_db -f backend/create_tables.sql
```

## Созданные файлы

- `backend/fix_content_tables.ps1` - PowerShell скрипт для создания таблиц
- `backend/check_tables.ps1` - PowerShell скрипт для проверки таблиц
- `backend/create_content_tables.py` - Python скрипт для создания таблиц
